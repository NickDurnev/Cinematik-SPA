import styled from '@emotion/styled';
import { motion } from 'framer-motion';

const Wrap = styled(motion.div)`
  width: 100%;
  padding: 20px;
  text-align: center;
  border-radius: 5px;
`;

export default Wrap;
